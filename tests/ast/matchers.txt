match compute kernel:

match cxxRecordDecl(isDefinition(),hasAttr(clang::attr::Annotate()))

dump ast 

clang -Xclang -ast-dump -fsyntax-only -std=c++20 -x c++ -I../../swizzleforge -fms-compatibility -fms-extensions GameOfLifeTest.h